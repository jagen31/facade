#lang info

(define collection 'multi)

(define version "0.0.1")

(define deps
  '("facade-lib"))
(define build-deps '())

(define implies '("facade-lib"))

(define pkg-desc "Art 4, in Rhombus -- the metapackage")

(define license '(MIT))
