#lang info

(define collection "facade")

(define version "0.0.1")

(define deps
  '("base"
    "rhombus-lib"))
(define build-deps '("rackunit-lib"))

(define pkg-desc "implementation (no documentation) part of \"facade\" -- Art 4, in Rhombus")

(define license '(MIT))
